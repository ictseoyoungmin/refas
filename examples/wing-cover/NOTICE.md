# Wing-cover fixture

The reference image is generated deterministically from `fixture.json` by `generate_reference.py`. The fixture and generated image are project-owned test material distributed under the repository's MIT License.

This is a workflow and regression fixture, not a claim that reconstruction from a synthetic image demonstrates general single-image inference quality.
