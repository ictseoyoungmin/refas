import {assertDigest, assertId, deepFreeze, digestJson} from './canonical.mjs';
import {
  PHYSICAL_ASSET_BUNDLE_SCHEMA,
  physicalAssetBundleIdentityProjection,
  validatePhysicalAssetBundle,
  validatePhysicalAssetBundleBindings,
} from './physical-asset-bundle.mjs';

export const REPRESENTATION_CAPACITY_SCHEMA = 'refas.representation-capacity/v1';
export const REPRESENTATION_CAPACITY_BUNDLE_BINDING_SCHEMA = 'refas.representation-capacity-bundle-binding/v1';
export const REPRESENTATION_SOURCE_KINDS = Object.freeze(['IDENTITY', 'COMPONENT']);
export const REPRESENTATION_APPROXIMATION_STRATEGIES = Object.freeze(['BAKED', 'REDUCED', 'SURROGATE', 'BACKEND_EXTENSION', 'CUSTOM']);

const SOURCE_KIND_SET = new Set(REPRESENTATION_SOURCE_KINDS);
const APPROXIMATION_STRATEGY_SET = new Set(REPRESENTATION_APPROXIMATION_STRATEGIES);
const CONTROL_RE = /[\u0000-\u001f\u007f]/u;
const SEMANTIC_PATH_RE = /^[a-z][a-z0-9-]*(?:\.[a-z][a-z0-9-]*)*$/u;
const TOP_LEVEL_KEYS = new Set(['schema','profileId','backend','bundleBinding','obligations','supported','approximated','unsupported','blockers','exportable','policy','capacityDigest']);
const BUNDLE_BINDING_KEYS = new Set(['schema','bundleSchema','bundleDigest','rootClosureDigest','rootModuleId','identityProjectionDigest']);
const OBLIGATION_KEYS = new Set(['obligationId','source','semanticPath','subjectIds']);
const IDENTITY_SOURCE_KEYS = new Set(['kind','projectionDigest']);
const COMPONENT_SOURCE_KEYS = new Set(['kind','componentId','schema','digest']);
const SUPPORTED_KEYS = new Set(['obligationId']);
const APPROXIMATED_KEYS = new Set(['obligationId','strategy','reason','retainedSemantics','lossSemantics']);
const UNSUPPORTED_KEYS = new Set(['obligationId','reason']);
const BLOCKER_KEYS = new Set(['blockerId','obligationIds','reason']);
const POLICY_KEYS = new Set(['bundleIsCanonicalInput','profileDoesNotOwnCanonicalTruth','obligationsDerivedFromCanonicalBundle','everyObligationClassifiedExactlyOnce','approximationMustBeExplicit','unsupportedSemanticsRemainExplicit','blockerPreventsExport','backendOrderingIsNotSemanticIdentity','profileDoesNotAuthorizeClaims','exporterImplementationRemainsDownstream']);
const CANONICAL_POLICY = Object.freeze({
  bundleIsCanonicalInput:true,
  profileDoesNotOwnCanonicalTruth:true,
  obligationsDerivedFromCanonicalBundle:true,
  everyObligationClassifiedExactlyOnce:true,
  approximationMustBeExplicit:true,
  unsupportedSemanticsRemainExplicit:true,
  blockerPreventsExport:true,
  backendOrderingIsNotSemanticIdentity:true,
  profileDoesNotAuthorizeClaims:true,
  exporterImplementationRemainsDownstream:true,
});

function assertRecord(value,label){if(!value||typeof value!=='object'||Array.isArray(value))throw new Error(`${label} must be an object`);return value;}
function assertKnownKeys(value,allowed,label){assertRecord(value,label);const extras=Object.keys(value).filter((key)=>!allowed.has(key));if(extras.length)throw new Error(`${label} contains unsupported field(s): ${extras.sort().join(', ')}`);}
function text(value,label,{maxLength=2048}={}){if(typeof value!=='string'||!value.length||value.trim()!==value||value.length>maxLength)throw new Error(`${label} must be a trimmed non-empty string up to ${maxLength} characters`);if(CONTROL_RE.test(value))throw new Error(`${label} must not contain control characters`);return value;}
function backend(value){return text(value,'backend',{maxLength:256});}
function semanticPath(value,label){const result=text(value,label,{maxLength:160});if(!SEMANTIC_PATH_RE.test(result))throw new Error(`${label} must be a canonical dotted semantic path`);return result;}
function idArray(value,label,{nonEmpty=true}={}){if(!Array.isArray(value))throw new Error(`${label} must be an array`);if(nonEmpty&&!value.length)throw new Error(`${label} must not be empty`);const ids=value.map((item,index)=>assertId(item,`${label}[${index}]`)).sort();if(new Set(ids).size!==ids.length)throw new Error(`${label} must contain unique IDs`);return ids;}
function stringArray(value,label,{nonEmpty=true}={}){if(!Array.isArray(value))throw new Error(`${label} must be an array`);if(nonEmpty&&!value.length)throw new Error(`${label} must not be empty`);const entries=value.map((item,index)=>text(item,`${label}[${index}]`,{maxLength:256})).sort();if(new Set(entries).size!==entries.length)throw new Error(`${label} must contain unique entries`);return entries;}
function uniqueIds(values){return [...new Set(values.filter((value)=>value!=null))].sort();}

function normalizeSource(raw,label){assertRecord(raw,label);const kind=String(raw.kind??'').trim().toUpperCase();if(!SOURCE_KIND_SET.has(kind))throw new Error(`${label}.kind must be one of: ${REPRESENTATION_SOURCE_KINDS.join(', ')}`);if(kind==='IDENTITY'){assertKnownKeys(raw,IDENTITY_SOURCE_KEYS,label);return{kind,projectionDigest:assertDigest(raw.projectionDigest,`${label}.projectionDigest`)}}assertKnownKeys(raw,COMPONENT_SOURCE_KEYS,label);return{kind,componentId:assertId(raw.componentId,`${label}.componentId`),schema:text(raw.schema,`${label}.schema`,{maxLength:160}),digest:assertDigest(raw.digest,`${label}.digest`)}}
export function representationObligationId(source,path,subjectIds){const normalizedSource=normalizeSource(source,'source'),normalizedPath=semanticPath(path,'semanticPath'),normalizedSubjects=idArray(subjectIds,'subjectIds');return assertId(`representation:${digestJson({source:normalizedSource,semanticPath:normalizedPath,subjectIds:normalizedSubjects}).slice(0,48)}`,'obligationId');}
function obligation(source,path,subjects){const subjectIds=uniqueIds(subjects);if(!subjectIds.length)return null;const semantic=semanticPath(path,'semanticPath');return{obligationId:representationObligationId(source,semantic,subjectIds),source,semanticPath:semantic,subjectIds};}
function normalizeObligation(raw,index){const label=`obligations[${index}]`;assertKnownKeys(raw,OBLIGATION_KEYS,label);const source=normalizeSource(raw.source,`${label}.source`),path=semanticPath(raw.semanticPath,`${label}.semanticPath`),subjectIds=idArray(raw.subjectIds,`${label}.subjectIds`),obligationId=assertId(raw.obligationId,`${label}.obligationId`),expected=representationObligationId(source,path,subjectIds);if(obligationId!==expected)throw new Error(`${label}.obligationId must be canonical ID ${expected}`);return{obligationId,source,semanticPath:path,subjectIds};}
function normalizeSupported(raw,index){const label=`supported[${index}]`;assertKnownKeys(raw,SUPPORTED_KEYS,label);return{obligationId:assertId(raw.obligationId,`${label}.obligationId`)}}
function normalizeApproximated(raw,index){const label=`approximated[${index}]`;assertKnownKeys(raw,APPROXIMATED_KEYS,label);const strategy=String(raw.strategy??'').trim().toUpperCase();if(!APPROXIMATION_STRATEGY_SET.has(strategy))throw new Error(`${label}.strategy must be one of: ${REPRESENTATION_APPROXIMATION_STRATEGIES.join(', ')}`);const retainedSemantics=stringArray(raw.retainedSemantics,`${label}.retainedSemantics`),lossSemantics=stringArray(raw.lossSemantics,`${label}.lossSemantics`),retained=new Set(retainedSemantics);for(const item of lossSemantics)if(retained.has(item))throw new Error(`${label} may not list the same semantic as both retained and lost: ${item}`);return{obligationId:assertId(raw.obligationId,`${label}.obligationId`),strategy,reason:text(raw.reason,`${label}.reason`),retainedSemantics,lossSemantics};}
function normalizeUnsupported(raw,index){const label=`unsupported[${index}]`;assertKnownKeys(raw,UNSUPPORTED_KEYS,label);return{obligationId:assertId(raw.obligationId,`${label}.obligationId`),reason:text(raw.reason,`${label}.reason`)}}
function normalizeBlocker(raw,index){const label=`blockers[${index}]`;assertKnownKeys(raw,BLOCKER_KEYS,label);return{blockerId:assertId(raw.blockerId,`${label}.blockerId`),obligationIds:idArray(raw.obligationIds,`${label}.obligationIds`),reason:text(raw.reason,`${label}.reason`)}}
function normalizePolicy(raw){assertKnownKeys(raw,POLICY_KEYS,'policy');if(digestJson(raw)!==digestJson(CANONICAL_POLICY))throw new Error('policy must equal the canonical P11 representation-capacity policy');return{...CANONICAL_POLICY};}

function bundleBindingFor(bundle){return{schema:REPRESENTATION_CAPACITY_BUNDLE_BINDING_SCHEMA,bundleSchema:PHYSICAL_ASSET_BUNDLE_SCHEMA,bundleDigest:bundle.bundleDigest,rootClosureDigest:bundle.rootClosureDigest,rootModuleId:bundle.rootModuleId,identityProjectionDigest:bundle.identityBinding.projectionDigest};}
function normalizeBundleBinding(raw){assertKnownKeys(raw,BUNDLE_BINDING_KEYS,'bundleBinding');if(raw.schema!==REPRESENTATION_CAPACITY_BUNDLE_BINDING_SCHEMA)throw new Error(`bundleBinding.schema must be ${REPRESENTATION_CAPACITY_BUNDLE_BINDING_SCHEMA}`);if(raw.bundleSchema!==PHYSICAL_ASSET_BUNDLE_SCHEMA)throw new Error(`bundleBinding.bundleSchema must be ${PHYSICAL_ASSET_BUNDLE_SCHEMA}`);return{schema:raw.schema,bundleSchema:raw.bundleSchema,bundleDigest:assertDigest(raw.bundleDigest,'bundleBinding.bundleDigest'),rootClosureDigest:assertDigest(raw.rootClosureDigest,'bundleBinding.rootClosureDigest'),rootModuleId:assertId(raw.rootModuleId,'bundleBinding.rootModuleId'),identityProjectionDigest:assertDigest(raw.identityProjectionDigest,'bundleBinding.identityProjectionDigest')}}

function componentSubjects(contract){switch(contract.schema){
case'refas.rigid-body-dynamics/v1':case'refas.collision-model/v1':return uniqueIds((contract.links??[]).map((item)=>item.linkId));
case'refas.articulation-graph/v1':return uniqueIds([contract.rootLinkId,...(contract.linkBindings??[]).map((item)=>item.linkId),...(contract.joints??[]).flatMap((item)=>[item.virtualJointId,item.parentLinkId,item.childLinkId])]);
case'refas.mechanism-graph/v1':return uniqueIds((contract.mechanisms??[]).flatMap((item)=>[item.mechanismId,...(item.realizedJointIds??[]),...(item.members??[]).map((member)=>member.physicalIdentityId)]));
case'refas.transmission-model/v1':return uniqueIds((contract.transmissions??[]).flatMap((item)=>[item.transmissionId,...(item.contextMechanismIds??[]),...(item.inputSpace?.coordinates??[]).map((coordinate)=>coordinate.semanticIdentityId),...(item.outputSpace?.coordinates??[]).map((coordinate)=>coordinate.semanticIdentityId)]));
case'refas.actuation-model/v1':return uniqueIds((contract.actuators??[]).flatMap((item)=>[item.actuatorId,item.drivenTargetId]));
case'refas.control-profile/v1':return uniqueIds((contract.profiles??[]).flatMap((item)=>[item.selector?.controllerId,item.selector?.actuatorId]));
case'refas.runtime-binding/v1':return uniqueIds((contract.bindings??[]).flatMap((item)=>[item.selector?.runtimeEndpointId,item.selector?.targetId]));
default:return[];}}

function componentPaths(contract){switch(contract.schema){
case'refas.rigid-body-dynamics/v1':return['dynamics.mass','dynamics.center-of-mass','dynamics.inertia'];
case'refas.collision-model/v1':return['collision.geometry','collision.filter','collision.self-policy'];
case'refas.articulation-graph/v1':return['articulation.topology','articulation.joint-frame','articulation.reference-configuration','articulation.joint-limit'];
case'refas.mechanism-graph/v1':return['mechanism.topology'];
case'refas.transmission-model/v1':{const paths=['transmission.coordinate-map','transmission.velocity-map','transmission.effort-map'];if((contract.transmissions??[]).some((item)=>['NONLINEAR','EXTERNAL_SOLVER'].includes(item.mapping?.kind)))paths.push('transmission.external-implementation');return paths;}
case'refas.actuation-model/v1':return['actuation.kind','actuation.position-range','actuation.velocity-limit','actuation.effort-limit','actuation.passive-dynamics','actuation.control-modes'];
case'refas.control-profile/v1':return['control.mode','control.command-space','control.gains','control.delay'];
case'refas.runtime-binding/v1':return['runtime.endpoint','runtime.locator','runtime.index','runtime.calibration','runtime.transport-delay'];
default:return[];}}

function buildLiveInventory(bundle,identityGraph,components){const intrinsic=validatePhysicalAssetBundle(bundle);if(!intrinsic.valid)throw new Error(`physical asset bundle is invalid: ${intrinsic.errors.join('; ')}`);const bindings=validatePhysicalAssetBundleBindings(bundle,{identityGraph,components});if(!bindings.valid)throw new Error(`physical asset bundle bindings are stale: ${bindings.errors.join('; ')}`);const identityProjection=physicalAssetBundleIdentityProjection(identityGraph,bundle.rootModuleId),bundleRefById=new Map(bundle.componentRefs.map((ref)=>[ref.componentId,ref])),liveById=new Map();for(const item of components??[]){if(!item||typeof item!=='object')continue;if(bundleRefById.has(item.componentId))liveById.set(item.componentId,item.contract);}return{identityProjection,bundleRefById,liveById};}

export function deriveRepresentationCapacityObligations({bundle,identityGraph,components=[]}={}){const inventory=buildLiveInventory(bundle,identityGraph,components),identitySource={kind:'IDENTITY',projectionDigest:bundle.identityBinding.projectionDigest},identity=inventory.identityProjection,items=[];const entities=identity.entities??[],relations=identity.relations??[];const allEntityIds=uniqueIds(entities.map((entity)=>entity.id));const framed=uniqueIds(entities.filter((entity)=>entity.frame!=null).map((entity)=>entity.id));const contains=uniqueIds(relations.filter((relation)=>relation.kind==='CONTAINS').flatMap((relation)=>[relation.sourceId,...relation.targetIds]));const interfaces=uniqueIds(entities.filter((entity)=>entity.kind==='attachment-interface').map((entity)=>entity.id));const compatible=uniqueIds(entities.filter((entity)=>(entity.compatibilityFamilyIds??[]).length).map((entity)=>entity.id));for(const [path,subjects] of [['identity.entity',allEntityIds],['composition.contains',contains],['composition.interface',interfaces],['frame.transform',framed],['compatibility.family',compatible]]){const entry=obligation(identitySource,path,subjects);if(entry)items.push(entry);}for(const ref of bundle.componentRefs){const contract=inventory.liveById.get(ref.componentId);if(!contract)throw new Error(`representation obligation derivation requires live component payload for ${ref.componentId}`);const source={kind:'COMPONENT',componentId:ref.componentId,schema:ref.schema,digest:ref.digest},subjects=componentSubjects(contract);if(!subjects.length)throw new Error(`component ${ref.componentId} exposes no semantic subjects for representation profiling`);for(const path of componentPaths(contract)){const entry=obligation(source,path,subjects);if(entry)items.push(entry);}}items.sort((a,b)=>a.obligationId.localeCompare(b.obligationId));const ids=new Set(),signatures=new Set();for(const item of items){if(ids.has(item.obligationId))throw new Error(`duplicate representation obligation ID: ${item.obligationId}`);ids.add(item.obligationId);const signature=digestJson({source:item.source,semanticPath:item.semanticPath,subjectIds:item.subjectIds});if(signatures.has(signature))throw new Error(`duplicate semantic representation obligation: ${item.semanticPath}`);signatures.add(signature);}if(!items.length)throw new Error('representation capacity requires at least one derived obligation');return deepFreeze(items);}

function normalizeDecisionSets(raw){if(!Array.isArray(raw.supported)||!Array.isArray(raw.approximated)||!Array.isArray(raw.unsupported)||!Array.isArray(raw.blockers))throw new Error('supported, approximated, unsupported, and blockers must be arrays');const supported=raw.supported.map(normalizeSupported).sort((a,b)=>a.obligationId.localeCompare(b.obligationId)),approximated=raw.approximated.map(normalizeApproximated).sort((a,b)=>a.obligationId.localeCompare(b.obligationId)),unsupported=raw.unsupported.map(normalizeUnsupported).sort((a,b)=>a.obligationId.localeCompare(b.obligationId)),blockers=raw.blockers.map(normalizeBlocker).sort((a,b)=>a.blockerId.localeCompare(b.blockerId));if(new Set(supported.map((item)=>item.obligationId)).size!==supported.length)throw new Error('supported contains duplicate obligationId values');if(new Set(approximated.map((item)=>item.obligationId)).size!==approximated.length)throw new Error('approximated contains duplicate obligationId values');if(new Set(unsupported.map((item)=>item.obligationId)).size!==unsupported.length)throw new Error('unsupported contains duplicate obligationId values');if(new Set(blockers.map((item)=>item.blockerId)).size!==blockers.length)throw new Error('blockers contains duplicate blockerId values');return{supported,approximated,unsupported,blockers};}
function validateCoverage(obligations,decisions){const ids=new Set(obligations.map((item)=>item.obligationId)),count=new Map(obligations.map((item)=>[item.obligationId,0])),supported=new Set(decisions.supported.map((item)=>item.obligationId));for(const entry of [...decisions.supported,...decisions.approximated,...decisions.unsupported]){if(!ids.has(entry.obligationId))throw new Error(`classification references unknown obligation: ${entry.obligationId}`);count.set(entry.obligationId,count.get(entry.obligationId)+1);}for(const[id,value]of count)if(value!==1)throw new Error(`obligation ${id} must be classified exactly once; found ${value}`);for(const blocker of decisions.blockers)for(const id of blocker.obligationIds){if(!ids.has(id))throw new Error(`blocker ${blocker.blockerId} references unknown obligation: ${id}`);if(supported.has(id))throw new Error(`blocker ${blocker.blockerId} may not block exactly supported obligation ${id}`);}}
function normalizePersistedProfile(value){assertKnownKeys(value,TOP_LEVEL_KEYS,'representation capacity profile');if(value.schema!==REPRESENTATION_CAPACITY_SCHEMA)throw new Error(`schema must be ${REPRESENTATION_CAPACITY_SCHEMA}`);const profileId=assertId(value.profileId,'profileId'),backendName=backend(value.backend),bundleBinding=normalizeBundleBinding(value.bundleBinding);if(!Array.isArray(value.obligations)||!value.obligations.length)throw new Error('obligations must contain at least one projection obligation');const obligations=value.obligations.map(normalizeObligation).sort((a,b)=>a.obligationId.localeCompare(b.obligationId));if(new Set(obligations.map((item)=>item.obligationId)).size!==obligations.length)throw new Error('obligations contains duplicate obligationId values');const decisions=normalizeDecisionSets(value);validateCoverage(obligations,decisions);if(typeof value.exportable!=='boolean')throw new Error('exportable must be boolean');if(value.exportable!==(decisions.blockers.length===0))throw new Error('exportable must equal blockers.length === 0');const policy=normalizePolicy(value.policy),payload={schema:value.schema,profileId,backend:backendName,bundleBinding,obligations,supported:decisions.supported,approximated:decisions.approximated,unsupported:decisions.unsupported,blockers:decisions.blockers,exportable:value.exportable,policy},capacityDigest=assertDigest(value.capacityDigest,'capacityDigest');if(digestJson(payload)!==capacityDigest)throw new Error('capacityDigest does not reproduce');return{...payload,capacityDigest};}

export function createRepresentationCapacityProfile({profileId,backend:backendName,bundle,identityGraph,components=[],supported=[],approximated=[],unsupported=[],blockers=[]}={}){const obligations=deriveRepresentationCapacityObligations({bundle,identityGraph,components}),decisions=normalizeDecisionSets({supported,approximated,unsupported,blockers});validateCoverage(obligations,decisions);const payload={schema:REPRESENTATION_CAPACITY_SCHEMA,profileId:assertId(profileId,'profileId'),backend:backend(backendName),bundleBinding:bundleBindingFor(bundle),obligations:[...obligations],supported:decisions.supported,approximated:decisions.approximated,unsupported:decisions.unsupported,blockers:decisions.blockers,exportable:decisions.blockers.length===0,policy:{...CANONICAL_POLICY}};return deepFreeze({...payload,capacityDigest:digestJson(payload)});}
export function validateRepresentationCapacityProfile(value){const errors=[];try{const normalized=normalizePersistedProfile(value);if(digestJson(normalized)!==digestJson(value))errors.push('representation capacity profile is not canonical');}catch(error){errors.push(error.message);}return{valid:errors.length===0,errors};}
export function validateRepresentationCapacityBindings(value,{bundle,identityGraph,components=[]}={}){const errors=[],intrinsic=validateRepresentationCapacityProfile(value);if(!intrinsic.valid)errors.push(`representation capacity profile invalid: ${intrinsic.errors.join('; ')}`);try{if(!errors.length){const bundleValidation=validatePhysicalAssetBundle(bundle);if(!bundleValidation.valid)throw new Error(`physical asset bundle is invalid: ${bundleValidation.errors.join('; ')}`);if(digestJson(bundleBindingFor(bundle))!==digestJson(value.bundleBinding))throw new Error('representation capacity bundle binding is stale');const expected=deriveRepresentationCapacityObligations({bundle,identityGraph,components});if(digestJson(expected)!==digestJson(value.obligations))throw new Error('representation capacity obligation inventory is stale or incomplete for the current bundle');}}catch(error){errors.push(error.message);}return{valid:errors.length===0,errors};}
export function representationCapacityDecision(profile,obligationId){const validation=validateRepresentationCapacityProfile(profile);if(!validation.valid)throw new Error(`representation capacity profile is invalid: ${validation.errors.join('; ')}`);const id=assertId(obligationId,'obligationId'),supported=profile.supported.find((entry)=>entry.obligationId===id);if(supported)return deepFreeze({status:'SUPPORTED',...supported});const approximated=profile.approximated.find((entry)=>entry.obligationId===id);if(approximated)return deepFreeze({status:'APPROXIMATED',...approximated});const unsupported=profile.unsupported.find((entry)=>entry.obligationId===id);if(unsupported)return deepFreeze({status:'UNSUPPORTED',...unsupported});return null;}
export function assertRepresentationCapacityExportable(profile){const validation=validateRepresentationCapacityProfile(profile);if(!validation.valid)throw new Error(`representation capacity profile is invalid: ${validation.errors.join('; ')}`);if(!profile.exportable)throw new Error(`representation capacity profile is blocked: ${profile.blockers.map((item)=>item.blockerId).join(', ')}`);return profile;}
